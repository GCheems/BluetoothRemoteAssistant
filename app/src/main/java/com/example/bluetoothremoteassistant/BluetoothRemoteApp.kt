package com.example.bluetoothremoteassistant

import android.app.Application

/**
 * 应用程序类
 */
class BluetoothRemoteApp : Application() {
    
    override fun onCreate() {
        super.onCreate()
        // 全局初始化逻辑（如需要）
    }
}
